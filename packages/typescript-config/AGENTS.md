# typescript-config package

- Scope: shared tsconfig presets used across this monorepo.
- Follow repository-wide guidance in `../../AGENTS.md`.
- Keep presets generic; add framework-specific config only when necessary.
