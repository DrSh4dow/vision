# ui package

- Scope: shared UI primitives, shadcn wrappers, and design tokens.
- Follow repository-wide guidance in `../../AGENTS.md`.
- Keep components presentation-focused; keep domain logic out of this package.
- Use `shadcn` tools when you need to add components from the shadcn registry.
