# storage

Local persistence and future sync hooks.

## Responsibilities

- IndexedDB / OPFS persistence primitives
- Import/export document snapshots
- Hooks/interfaces for future cloud sync/collab

## Notes

- Keep sync concerns behind interfaces so they can evolve later
