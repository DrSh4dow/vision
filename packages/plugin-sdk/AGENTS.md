# plugin-sdk package

- Scope: stable public plugin types and helper APIs.
- Follow repository-wide guidance in `../../AGENTS.md`.
- Treat exports as compatibility contracts; prefer additive, backward-compatible changes.
