/** Scale factor: 1 mm in design space = this many canvas units (pixels at zoom 1). */
export const MM_TO_PX = 10;

/** Minimum zoom level. */
export const ZOOM_MIN = 0.01;

/** Maximum zoom level. */
export const ZOOM_MAX = 256;

/** Minimum drag distance in mm to create a shape. */
export const MIN_DRAG_MM = 1;

/** Zoom factor per wheel tick. */
export const ZOOM_FACTOR = 1.1;
