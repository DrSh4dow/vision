# vision-web

- Scope: Vite React browser UI for Vision (currently a minimal scaffold in `src/App.tsx`).
- Follow repository-wide guidance in `../../AGENTS.md`.
- Keep domain and engine logic in shared packages (`packages/core`, `packages/runtime`, `packages/engine`).
