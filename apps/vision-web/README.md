# vision-web

The browser UI for Vision.

## Responsibilities

- Canvas, tools, inspectors, and panels
- Object editing + sequence view
- Plugin management UX (registry + sideload)
- Calls into `@vision/engine` (WASM façade) and `@vision/core` (document model)

## Dev

```bash
bun install
bun run dev
```

## Build

```bash
bun run build
```
